# User-Registration-Problem
The User Registration Problem contains list of usecase scenarios specifically curated for understanding Regex in python using the re module.Each succesive usecase ,has a validity check which the user needs to pass as a basic requirement for getting registered in the system.
